select  datname from pg_database

create table employee (
employee_id int ,
employee_name varchar(30),
employee_salary int ,
employee_department varchar(30)
);

select * from employee 

/* add column in a table * /
 
 alter table employee add column mobile_no varchar(30);
 alter table employee add column city varchar (30);
 
 /* rename column * /

alter table employee rename city to location ;

alter table employee rename location to city;
 
alter table employee rename employee_salary to emp_salary ; 

alter table employee rename employee_id to emp_id ;

alter table employee rename  employee_name to emp_name ;

alter table employee rename  employee_department to emp_department;

alter table employee rename   employee_department to emp_department ;

/*   insert values * / 
 
 insert into  employee values
 (1, 'ansh', 20000, 'IT', '7689675434','hyd'),
 (2, 'lamba', 25000, 'HR', '989675890','kolkata'),
 (3,'maxwel',30000,'Sales','89675434345','hyd'),
 (4,'vetory',40000,'IT','996754343','vizag'),
 (5,'philips',50000,'HR','9075434345','vizag');
 
 / * update the values * /
  
  update employee set emp_name = 'bara' where emp_id = 2;
  delete  from employee where emp_name = 'bara' ;
  delete  from employee where emp_name = 'ansh' ;
  delete  from employee where emp_name = 'maxwel' ;
  
  /* truncate means delete all the data but not structure of the table , structure won't change */
   
  truncate table employee ;

  / * DQL data query language which is used to fetch data * /
   
    select * from employee;
  
  
  
  
  




