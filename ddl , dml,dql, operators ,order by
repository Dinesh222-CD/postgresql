CREATE TABLE employee (
    emp_id INT,
    name VARCHAR(50),
    department VARCHAR(30),
    salary INT,
    age INT
);

/* INSERT INTO employee VALUES */

(1, 'Alice', 'IT', 50000, 25),
(2, 'Bob', 'HR', 30000, 30),
(3, 'Charlie', 'Sales', 25000, 28),
(4, 'David', 'IT', 45000, 35),
(5, 'Eva', 'Finance', 60000, 40),

(6, 'Frank', 'Sales', 20000, 26),
(7, 'Grace', 'IT', 55000, 29),
(8, 'Hank', 'HR', 32000, 31),
(9, 'Ivy', 'Finance', 70000, 38),
(10, 'Jack', 'Sales', 27000, 27),

(11, 'Kate', 'IT', 48000, 33),
(12, 'Leo', 'HR', 35000, 36),
(13, 'Mia', 'Finance', 65000, 42),
(14, 'Nina', 'Sales', 22000, 24),
(15, 'Oscar', 'IT', 52000, 37),

(16, 'Paul', 'HR', 28000, 34),
(17, 'Quinn', 'Finance', 72000, 41),
(18, 'Rose', 'Sales', 26000, 23),
(19, 'Steve', 'IT', 53000, 39),
(20, 'Tom', 'HR', 31000, 32);
select * from employee where salary >50000;
select * from employee where salary <50000;
select * from employee where salary between 25000 and 50000;
select * from employee where salary between 15000 and 20000;
select * from employee where salary between 25000 and 30000;
select * from employee where salary between 30000 and 50000;
select * from  employee where salary between 50000 and 60000;
select * from employee where salary>=60000;
select * from employee where salary <=60000;

/* i want only particular name persons salary*/

SELECT salary 
FROM employee 
WHERE name = 'David';
  
select age  from employee where name ='hank';
select age  from employee where emp_id =1;
SELECT age 
FROM employee 
WHERE lower(name) = 'hank';

select age  from employee where lower(name)='bob';

select name,age,salary from employee;
select name,department, salary from employee;

/* where condition */
select name,department,salary from employee where department='IT';
select name,department,salary from employee where department='Sales';
select name , department , salary from employee where salary>50000;
select name , department , salary from employee where salary<30000;
select name , department , salary from employee where salary!=50000; 
select name , department , salary from employee where department='HR';
select name , emp_id , salary from employee where department ='IT';
select name , department , salary from employee where emp_id=1;

/* Logical Operators*/
select name , department, emp_id from employee where salary>20000 and age>25;
select name , salary, emp_id from employee where department='HR' and age>30; 
select * from employee where department!='HR' and salary>50000;
select * from employee where salary>60000 or age>35;
select name , salary , department from employee where not age<35;
SELECT name, department, salary 
FROM employee
WHERE department IN ('IT','Finance');
select salary, department from employee where age in ('25','35');
select salary, department from employee where name in ('Alice','Bob','David');

select salary , department  from employee where name='Alice'or name='Bob'or name='David';  
select name , department from employee where salary between 50000 and 70000;

SELECT name, department, salary 
FROM employee
WHERE name LIKE 'A%';

SELECT name, department, salary ,age
FROM employee
WHERE name LIKE '%a';

select name , salary,department from employee where name like '%e';

select name , age , salary from employee where name like'%n%';

select name , salary, department from employee order by salary Desc;
select salary , department , age from employee order by salary desc;
select salary , name , age from employee where department='HR' order by salary Desc;
select salary , name , age from employee where department='HR' order by age Desc;

select * from employee
/* substring */

select substring('god is good all the time' from 8 for 4);

/* min and max */

select min(salary) from employee;

select min(salary) from employee where department ='HR';

select max(salary) from employee;

select max(salary) from employee where department='Sales';

select  min(age) from employee;

select max(age)  from employee;

select max(salary) from employee where department = 'Sales' and salary>20000;

select max(salary) from employee where name in('Alice','Bob','Charlie');

----------------------------------------------------------------------------------
22/04/2026 (practice)
----------------------------------------------------------------------------------
select * from employee;
select * from employee order by name asc ;
select * from employee order by name desc ;
select * from employee order by salary asc;
select * from employee order by department asc;
select * from employee order by emp_id  asc ,  salary asc; 
select department ,count(*) from employee  group by department ;
select count(*) from employee ;

/* Show all employees with salary greater than 30,000 * /

select salary from employee where salary > 30000;

/* Show employees whose salary is between 30,000 and 60,000 */
 
 select salary from employee where salary between 30000 and 60000 ;
 select salary , name from employee where salary between 30000 and 60000  ;
 select salary , name , age from employee where name in ('Alice','Bob','Grace');
 select salary , name , age from employee where name in ('Alice','Bob','Grace') order by name asc, salary asc;
 select salary , name , age from employee where name in ('Alice','Bob','Grace') order by name desc, salary desc;

select salary, name  from employee where salary between 30000 and 60000 order by name asc , salary asc ;

/* Show employees whose department is 'IT' OR 'HR'  */
 
 select * from employee where department = 'IT' or department = 'HR';
 
 / *  Using IN (best & clean way 👍) */

select name , department from employee where department in ('IT','HR');

select * from employee where department = 'IT' or department = 'HR'order by department asc , salary asc;

/ * --Show employees whose name starts with 'A' */

select * from employee where name like 'A%';

select * from employee where name like 'B%' or name like 'A%'  ;

select * from employee where name like 'B%' or name like 'A%' order by name desc;

/*  --Show employees whose name have letter 'a'  */

select * from employee where name like '%a%';

select * from employee where name like '%a%' order by name asc;

select * from employee where name like '%a%' order by name desc;

select * from employee where name like '%C%' order by name asc;


/* Show employees whose salary is NOT equal to 40,000 */

select * from employee where salary != 40000 ;

select * from  employee where salary >= 40000;

select * from employee where salary <= 40000 ;

/* Show employees whose emp_id is in (2, 5, 8) */

select emp_id from employee where emp_id in(2,5,8);

select emp_id from employee where emp_id in(2,5,8);

select emp_id , name, salary  from employee where  department  in ('IT') ;

select emp_id , name , salary from employee where department in ('Sales');


/* Show employee id,employee name ,salary ,annual salary and department */

select emp_id , name , salary ,  salary*12 as annual_salary, salary department from employee ; 

select emp_id , name , salary ,  salary*12 as annual_salary, (salary*12)/365 as daily_salary ,min(salary) as minimum_salary, max(salary) as maximum_salary , order by salary desc as rank , salary department from employee; 


select emp_id , name , salary , salary/12 as monthly_salary , salary/365 as daily_salary , department from employee;

SELECT * from 
( 
     select  
       emp_id,
       name,
       salary as monthly_salary,  
       (salary*12) as annual_salary, 
       (salary*12)/365 as daily_salary ,
       Dense_rank() over(order by(salary*12)/365 desc) as D_rank
        from employee) as D
where D_rank=7;



select emp_id,name,department,salary as monthly_sal,
       (salary*12) annual_sal,
       (salary*12)/365 daily_sal,
       dense_rank()over(order by(salary*12)/365 desc) as D_R
       from employee;







